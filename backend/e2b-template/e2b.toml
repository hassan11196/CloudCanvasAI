# E2B template configuration for CloudCanvasAI document sandbox
# See https://e2b.dev/docs/sandbox-template for documentation

# Template ID will be assigned after first build
template_id = ""

# Dockerfile to use for building the template
dockerfile = "Dockerfile"

# Template name/alias
template_name = "cloudcanvasai-docs"

# Start command (optional)
# start_cmd = ""
